#include<stdio.h>

int main(void){

    int x = 83;
    int y = x + 39;

    printf("xの値は%d。", x);
    printf("yの値%d。\n", y);

    return 0;
}

/* memo

    変数の宣言: 初期化子を与えて初期化を行う。

    初期化: 変数の生成時に値を入れる。
    代入: 生成ずみの変数に値を入れる。
*/